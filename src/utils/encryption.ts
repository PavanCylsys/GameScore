// Encryption utilities - hash, compare, encrypt/decrypt
export function hash(value: string): string {
  // Implement hashing (e.g. bcrypt)
  return value;
}

export function compare(value: string, hashed: string): boolean {
  // Implement compare
  return value === hashed;
}
